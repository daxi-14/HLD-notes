# HLD-notes
Notes for HLD learning

the two folders are NOT mirrors of each other. Many same-named files have genuinely different content and coverage. Summary:
LectureDiff finding1–2 (101 & Consistent Hashing)notess version (2711w) covers DNS, sharding, consistent hashing in full. HLD version (1528w) is a richer narrative form (del.icio.us story, IP/DNS deep-dive) but stops before consistent hashing. Complementary, not duplicate. HLD/Load Balancer.md (HLD-only) adds Algorithms, Listeners, Routing, Target Groups, Health Checks, Security — substantial unique content for Lecture 2.3 (Caching)Very similar structure. HLD version uniquely covers Write-through / Write-back / Write-around cache strategies with definitions, and adds a SUMMARY section. HLD/Scaling with Redis.md (HLD-only) adds Redis eviction policies, cluster mode, sharding & replication config — unique depth for Lecture 3.5 (Facebook News Feed)Both Caching Contd files cover newsfeed. Content is nearly the same; notess has a slightly more complete fan-out section.6 (CAP + Master-Slave)notess CAP file has the main CAP + PACELC content. HLD CAP file is mostly an extra readings appendix — almost no body content of its own. However, HLD/Master Slave_ MySQL.md (HLD-only, 2196w) is a rich deep-dive: binlog, relay log, IO threads, sync vs async replication, statement-based vs row-based, replication lag, OLTP→OLAP pipeline. Entirely unique content.7 (SQL vs NoSQL)Same heading structure, very similar word counts (~3100w each). Both cover all sharding key examples (Banking, Slack, IRCTC, Twitter, Sports, Uber). HLD version has a more detailed Column-Family explanation with pagination use case. Largely overlapping with minor additions each side.8 (Database Orchestration)notess/NoSQL contd.md maps here (consistent hashing recap, manual sharding). HLD/Database Orchestration.md (HLD-only, 255 lines) is significantly richer: Seamless Shard Creation with Staging/Real phases, Standby Machine utilization estimates, Multi-Master read/write operations. Unique content that must be included.9–10 (Typeahead)Similar overall. HLD version adds a dedicated "Sharding the trie" section with prefix-based sharding key logic. notess has a better framing of "Can we reduce writes" as a lead-in to Threshold/Sampling. Complementary.11 (NoSQL Internals)Very similar — both cover WAL, LSM concept, key-value storage, append-only writes. HLD version is told more narratively; notess has better structured headings. Merge without much loss.12 (Messaging)Near-identical in structure and content. Minor prose differences.13 (Kafka + ZooKeeper)notess + HLD/ZK+Kafka are similar. HLD/Kafka .md (HLD-only, 1101w) is a standalone Kafka deep-dive: Producer, Cluster, Topic, Partition, Consumers, Multiple Partitions, Fault-tolerant replication — unique, structured differently from the ZK notes.14 (Elasticsearch)Near-identical heading structure and content. Both cover Lucene, stemming, tokenization, sharding by doc ID, replication.15–16 (S3 + Uber)S3/QuadTree files very similar; HLD adds HDFS + GeoHash reference links. Uber files nearly identical in content; HLD version has a Geohash references section with YouTube + Wikipedia links.17 (Rate Limiter + Unique ID)Near-identical structure and content across both files.18 (Hotstar)Only notess file available as readable source. The HLD/ PDF is skipped.19–20 (Microservices)Major divergence — the three files cover almost entirely different material: notess/Microservices.md = Shipkart lifecycle story (Phases 1–N, monolith evolution, Myntra example). HLD/Microservices 1.md = SOA vs microservices, API Gateway, Flipkart example, Observability, Distributed Tracing — content not in notess at all. HLD/Microservices 2.md = Two-Phase Commit, SAGA (Orchestration + Choreography), CQRS, Circuit Breaker — entirely unique. All three must be merged.
